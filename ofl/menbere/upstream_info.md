# Menbere — Source Metadata Investigation

**Model**: Claude Opus 4.6
**Date**: 2026-03-12

## Repository

- **URL**: https://github.com/SorkinType/Menbere
- **Default branch**: `main`
- **Pinned commit**: `ab34feb55b02536fd40281b7651ddafbfcabc544`
- **Commit date**: 2025-06-04
- **Commit message**: "Merge pull request #2 from emmamarichal/main — a few fixes"
- **Commit author**: Eben Sorkin
- **Repo created**: 2024-11-17
- **Last updated**: 2026-01-07

## Source Files

- **Primary source**: `sources/Menbere.glyphspackage` (Glyphs package format, split into `fontinfo.plist`, `glyphs/`, `order.plist`)
- **Additional source assets**: `sources/CustomFilter_GF_Latin_All.plist`, `sources/Images/`
- **Output fonts**:
  - `fonts/variable/Menbere[wght].ttf` — variable font (wght axis: 100–700)
  - Static TTFs built to `fonts/ttf/` (not tracked in METADATA.pb `files` block)
- **Scripts**: `scripts/` directory contains `customize.py`, `read-config.py`, `update-custom-filter.py`, `index.html`
- **Documentation**: `documentation/ARTICLE.en_us.html`
- **License**: `OFL.txt`

## Build System

- **Build tool**: `gftools builder` (via `make build`)
- **Build file**: `Makefile` (standard Google Fonts project template)
- **CI**: GitHub Actions workflow `.github/workflows/build.yaml` — builds and tests on every push using `gftools builder sources/config.yaml`
- **Python environment**: managed via `requirements.txt` / `requirements-test.txt` with `pip-tools`
- **Test runner**: `fontbakery check-googlefonts` (invoked via `make test`)
- **Autohinting**: `autohintTTF: true`, `ttfaUseScript: true` (script-aware autohinting for Ethiopic)

## config.yaml Status

- **Present**: yes — `sources/config.yaml`
- **Correctly referenced in METADATA.pb**: yes — `config_yaml: "sources/config.yaml"`
- **Key settings**:
  - `sources: [Menbere.glyphspackage]`
  - `buildVariable: true`, `buildStatic: true`, `buildOTF: false`
  - `autohintTTF: true`, `ttfaUseScript: true`, `cleanUp: true`
- **STAT table**: not explicitly defined in config.yaml; likely auto-generated by gftools builder

## Notes

- Menbere is an Ethiopic + Latin variable font covering wght 100–700. "Menbere" (መንበረ) is an Ethiopic word meaning "throne" or "seat".
- The family supports `ethiopic`, `latin`, `latin-ext`, and `vietnamese` subsets.
- Designer credits: Aleme Tadesse, Sorkin Type, Eben Sorkin.
- The repo follows the standard Google Fonts project template structure closely.
- PR #2 (merged at the pinned commit) was contributed by Emma Marichal and added glyph coverage: `verticallineabovecomb`, `verticallinebelowcomb`, `verticallinemod`, and fixed accent positioning.
- No upstream cache found at `/mnt/shared/upstream_repos/fontc_crater_cache/SorkinType/Menbere`.
- The `DESCRIPTION.en_us.html` file referenced in the `files` block of METADATA.pb is not present in the `ofl/menbere/` directory on the working copy; however an `article/ARTICLE.en_us.html` file is correctly mapped via the `files` block.
- Confidence in metadata accuracy: **high** — the pinned commit is the current HEAD of `main`, config.yaml is present and correctly referenced, and the built variable font path matches what METADATA.pb expects.
